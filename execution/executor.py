"""
Order executor — proprietary, not public.

Handles authenticated order placement on Polymarket CLOB
(market orders, FOK, position sizing).

Public interface:
    Executor.place_order(opportunity: Opportunity) -> bool
"""
# Core implementation is not included in this public repository.
