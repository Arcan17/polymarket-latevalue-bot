"""
Volatility estimator — proprietary, not public.

Computes realized annualized volatility from recent price data
used as input to the pricing model.

Public interface:
    estimate_vol(prices: list[float], window_s: int) -> float
"""
# Core implementation is not included in this public repository.
